// import React from 'react';

// class TaskDeleteButton extends React.Component {

    // constructor(props) {
    //     super(props);
    
    //     this.onDeleteClicked = this.onDeleteClicked.bind(this);
    // }

        // alert(`You have completed ${this.props.taskDescription}!`)
//         //when this button is clicked, I want it to iterate through the array and filter out all of the elements that don't match the id to a new array.
//                 //I can do this by defining the function in the app.js file (deleteTask), passing it through the children to the TaskDeleteButton.js file
                
//         //I then want to set this new array to the app state.

//     onDeleteClicked () {
//         alert(`You have deleted ${this.props.taskDescription}!`)
        
//     }

//     render() {
//         return (
//             <div>
                // <input className="btn btn-primary" type="submit" value="Delete" onClick={this.onDeleteClicked} />
//             </div>
//         );
//     }
// };

// const styles = {
//     paddingTop : "10px",
//     paddingBottom : "10px"
// };

// export default TaskDeleteButton;